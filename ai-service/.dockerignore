# Virtual environments (dependencies are installed from requirements.txt)
.venv
venv
env
ENV

# Byte-compiled files
__pycache__
*.py[cod]
*$py.class

# Local secrets — injected at runtime via docker-compose env_file / environment
.env
.env.*
!.env.example

# Tooling caches
.pytest_cache
.mypy_cache
.ruff_cache
.coverage
htmlcov

# Packaging artifacts
build
dist
*.egg-info
.eggs

# Docs / orchestration (not needed inside the image)
README.md
Dockerfile
.dockerignore
